# OpenCode2 integration

This directory contains OpenCode2 invocation metadata and thin adapters only. Canonical core, role,
workflow, and provider policy lives under `.agents/` and is explicitly composed by the orchestrator.
OpenCode workers do not require root `CLAUDE.md` or unrelated product/roadmap documents.
