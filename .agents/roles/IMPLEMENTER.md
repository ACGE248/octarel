# Implementer role

Own one dedicated write worktree. Implement only the bounded task, preserve existing contracts and data,
avoid unrelated refactors, run focused inner-loop checks, reconcile relevant maintained documentation, and
finish with the risk-selected exact-tree local gate. A checkout has only one write-capable owner.
