# UI audit workflow

Use the existing Playwright/axe infrastructure against the affected product or Control Center surface.
Inspect light/dark themes, hierarchy, spacing, responsive widths, states, destructive clarity, keyboard and
accessibility behavior. Report Critical/Major/Minor findings with concrete fixes. Audit scope follows
`.agents/core/TESTING.md`; this workflow is read-only unless a separate implementation task grants writes.
