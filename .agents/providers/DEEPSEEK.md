# DeepSeek provider adapter

DeepSeek is optional overflow through OpenCode2. It is disabled/manual by default and may not be selected
automatically. Use requires explicit repository-data and cost authorization plus an eligible role; its API
or billing path must never be enabled by fallback.
